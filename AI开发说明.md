# AI 开发版说明

- 本目录复制自“安全基础信息平台四期-原功能复刻版”。
- 原功能复刻版保持不变，后续功能新增、页面修改和交互调整统一在本目录进行。
- 保持 Axure 导出结构：根目录独立 HTML 页面，以及 `data`、`files`、`images`、`plugins`、`resources` 等依赖目录。
- 新增页面时，应同步维护页面 HTML、对应资源目录、入口及页面跳转。
- 每次修改后应通过本地 HTTP 服务检查页面显示和交互，避免直接使用 `file://` 导致脚本功能异常。

当前入口：`模块入口.html`

## 变更记录

### 2026-07-10：新增不符合项管理

- 在 HSE 监测管理下新增独立页面 `不符合项管理.html`。
- 从任务结果查询的行操作中移除“整改详情”，增加“不符合项管理”入口。
- 支持关键字、评估对象、评估形式、不符合项类型、整改状态、超期状态、计划完成时间查询。
- 支持列表展示、全选、CSV 导出、整改详情跳转及流转记录交互提示。
